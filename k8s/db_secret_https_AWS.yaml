apiVersion: v1
kind: Secret
type: Opaque # 범용

# Secret 기본 정보
metadata:
  name: my-app-secrets # Secret 이름

# Key, Value 형식으로 값 저장
stringData:
  DB_HOST: REMOVED
  DB_PORT: "REMOVED"
  DB_NAME: REMOVED
  DB_USERNAME: REMOVED
  DB_PASSWORD: REMOVED
